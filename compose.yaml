services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=tms-project'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5434:5432'
